import logo from './assets/logo.svg'

const cartWidget = () => {
    return (
        <div>
            <img src={logo} alt="cart-widget"/>
            0
        </div>
    );
}

export default cartWidget